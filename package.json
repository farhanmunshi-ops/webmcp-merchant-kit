{
  "name": "packrift-webmcp",
  "version": "0.1.0",
  "private": true,
  "description": "Merchant-defined WebMCP tools for packrift.com, built on webmcp-merchant-kit — the extension layer for Shopify's built-in storefront WebMCP.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python3 -m http.server 8123"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "license": "MIT"
}
